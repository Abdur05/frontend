import { DecimaldirectiveDirective } from './decimaldirective.directive';

describe('DecimaldirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimaldirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
