import { FourdirectiveDirective } from './fourdirective.directive';

describe('FourdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FourdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
